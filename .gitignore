# build output (site/dist, podcast/public)
dist/
.astro/
podcast/public/
# dependencies
node_modules/
__pycache__/
*.pyc
# secrets: the Oracle API key, the server ssh key. Copies live in 1Password. Never `git add -f`.
.secrets/
# podcast audio: 1.7 GB and growing; the laptop and the server are the copies
podcast/shows/*/media/
# environment variables
.env
.env.production
# logs
npm-debug.log*
# macOS / editors
.DS_Store
.vscode/
.idea/
