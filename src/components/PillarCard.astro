---
const { href, eyebrow, title, body, cta = 'Explore →' } = Astro.props;
---
<a class="card" href={href}>
  <span class="card-eyebrow">{eyebrow}</span>
  <h2>{title}</h2>
  <p>{body}</p>
  <div class="more">{cta}</div>
</a>
